// @ts-check
import { defineConfig } from 'astro/config';

// Todavía no hay dominio. Cuando exista, cambiarlo acá y crear public/CNAME.
export default defineConfig({
  site: 'https://paseohygge.com',
});
